[project]
name = "paraegox-textual"
version = "0.1.0"
description = "Optional Textual chat client for Paraegox"
requires-python = ">=3.9"
dependencies = [
    "textual==8.2.8",
]

[tool.uv]
package = false
